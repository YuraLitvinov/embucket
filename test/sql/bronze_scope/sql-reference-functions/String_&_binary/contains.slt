query T
WITH strings_test(s) AS (SELECT * FROM
  VALUES
    ('coffee'),
    ('ice tea'),
    ('latte'),
    ('tea'),
    (NULL)
)
SELECT * FROM strings_test WHERE CONTAINS(s, 'te')
----
ice tea
latte
tea

query TT
SELECT CONTAINS('ñ', 'n'),
       CONTAINS('nñ', 'n')
----
FALSE	TRUE
